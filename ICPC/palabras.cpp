#include <bits//stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    for (int i = 0; i < n; i+=) {
        string palabra;
        cin >> palabra;

        int largo = palabra.
    }
}